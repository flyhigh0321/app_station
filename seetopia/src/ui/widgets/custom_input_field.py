from kivymd.uix.textfield import MDTextField
from utils import utils as rp  # ...
from kivy.lang import Builder

# kv = rp.relative_to_abs_path("src/ui/widgets/kv/custom_input_field.kv")


class CustomInputField(MDTextField):
    """An instance of :kivy_text:`kivy.uix.textinput <>`

    .. note::
        MDTextField inherited from ``TextInput``. Therefore, most parameters
        and all events of the ``TextInput`` class are also available in the
        ``MDTextField`` class.
    """

    pass
